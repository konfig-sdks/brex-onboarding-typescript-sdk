import { BrexOnboarding } from "./index";

describe("brex-onboarding-typescript-sdk", () => {
    it("initialize client", async () => {
        const brexonboarding = new BrexOnboarding({
        });
    });
});
